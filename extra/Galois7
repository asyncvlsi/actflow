--- libgalois/src/HWTopoLinux.cpp
+++ libgalois/src/HWTopoLinux.cpp
@@ -100,7 +100,7 @@ unsigned getNumaNode(cpuinfo& c) {
 std::vector<cpuinfo> parseCPUInfo() {
   std::vector<cpuinfo> vals;
 
-  const int len = 1024;
+  const int len = 10240;
   std::array<char, len> line;
 
   std::ifstream procInfo("/proc/cpuinfo");
